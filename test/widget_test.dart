import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Extension loads without error', (WidgetTester tester) async {
    // DevTools extensions require the DevTools environment to function properly.
    // Unit tests for individual components should be added separately.
    expect(true, isTrue);
  });
}
