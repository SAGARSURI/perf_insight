name: perf_insight
description: "AI-Powered Memory Analysis for Flutter DevTools. Stop guessing about memory issues — get actionable recommendations powered by LLMs."
publish_to: 'none'
version: 1.0.13

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  # DevTools extension framework
  devtools_extensions: ^0.4.0
  devtools_app_shared: ^0.4.0

  # Dart Tooling Daemon for file system access
  dtd: ^4.0.0

  # VM Service for performance data
  vm_service: ^15.0.0

  # HTTP client for API calls
  http: ^1.2.0

  # State management
  flutter_riverpod: ^2.6.0

  # Local storage for settings
  shared_preferences: ^2.3.0

  # JSON serialization
  json_annotation: ^4.9.0

  # Charts for visualization
  fl_chart: ^0.69.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  build_runner: ^2.4.0
  json_serializable: ^6.8.0

flutter:
  uses-material-design: true
